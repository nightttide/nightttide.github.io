tasks:
  - before: |
      npm install -g http-server
      npm install three
    init: |
    command: |
      chmod +x gitpod-init.sh && ./gitpod-init.sh
ports:
  - port: 8000
  - port: 8080
    onOpen: open-preview